---
description: 仅当从 Wei-Shaw/sub2api 做上游同步时生效。普通功能/修 bug 不要求隔离 worktree，在占用 checkout 上改即可。同步任务禁止整仓合入、整文件覆盖。
alwaysApply: true
---

# 上游同步 MUST（仅限上游同步任务）

**本文件始终在场，是为了防止漏掉同步硬禁止。它不是默认工作区政策。**

普通功能 / 修 bug / 写文档：在占用 checkout `E:\cursor project\api2sub` 上直接改（`main` 或任务要求的功能分支）。不要另开隔离 worktree。不要把下面 Workspace 句子抄进 Trellis `implement.md` / PRD / design。

仅当任务**确实是**从 `Wei-Shaw/sub2api` 取更新（或移植旧同步分支 / 开同步窗口）时，才执行下面整份 playbook。

**单独提到这些词，不要激活本 playbook：** `worktree`（包括「不要用 worktree」）、网关「同步超时」/`stream:false`/524、HTTP「上游」账号或响应。这些是普通任务。

真同步信号（须同时满足「从 Wei-Shaw/sub2api 取更新」）：「上游同步」/ `upstream sync` / 同步窗口 / 合并副本（同步）/ 移植旧 catchup 分支。单独的 `worktree`、`sync`、`upstream` 不够。

- MUST 先出评估表并等确认，再改**同步相关**产品代码。
- 按三车道处置，禁止一股脑全收。
- **同步任务中**：占用中的 `E:\cursor project\api2sub` 保持 `main`（不要为同步把它切走）。同步产品代码只写隔离 worktree。规则/文档可写在该 checkout（不切分支）。普通任务忽略本条。

| 车道 | 收什么 |
|------|--------|
| A 热修 | 协议 / 失败分类 / 身份 / 换号；不碰展示计费 |
| B 三维 | gateway / scheduler / usage 等同文件；先读本仓二开再叠 |
| C 延后/拒绝 | Grok 整包、monitor v2、Passkey、plaza、换部署等 |

```text
❌ BAD: git merge upstream/main，或整文件替换 openai_gateway_service.go
❌ BAD: 同步任务中，在占用 checkout 切走 main、或把同步产品代码写在占用 checkout
❌ BAD: 普通修 bug 却去建隔离 worktree，或把本段抄进 Trellis implement.md
❌ BAD: 删除旧 catchup worktree，或把它 merge/rebase 进 main / 新副本
❌ BAD: 功能冲突时自行删二开或选边上游
✅ GOOD（普通任务）: 在占用 checkout 上改产品代码（main 或功能分支）
✅ GOOD（同步任务）: 评估表 → 隔离 worktree → 按文件精细叠行为 → 冲突先问 Brandon
✅ GOOD: 旧 catchup worktree/分支只当补丁源，不删、不整仓合入
```

## 硬禁止（仍只约束上游同步交付）

- 整仓 `merge upstream/main` 当交付；整段/整文件用上游或旧同步分支覆盖热路径。
- 未读本仓用法就改共享文件（展示计费 / `actual_cost` / cache-read / bridge / 调度 pair / TTFT / migration 号）。
- 解不了的功能冲突不先问 Brandon。
- 把旧同步分支（如 `sync/upstream-v0152-to-v0170`、`sync/upstream-catchup-*`）直接 merge/rebase 进 `main` 或新副本。
- 删除旧 catchup worktree（如 `E:\cursor project\api2sub-upstream-catchup-20260814`）。旧产物只当补丁源。
- 新增 SQL 复用 `main` 已占用号，或改历史 migration。编号必须从当时 `main` max+1 起。
- 未获当次明确授权：把同步分支合进真正的 `main`、`git push`、生产部署。
- 把本仓 `VERSION` 改成上游版本号。

评估表必填列与二开影响面见 `AGENTS.md`「Upstream sync」。细则以 `AGENTS.md` 为准。
