// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Wei-Shaw/sub2api/ent/aicreditsnapshot"
)

// AICreditSnapshot is the model entity for the AICreditSnapshot schema.
type AICreditSnapshot struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// Email holds the value of the "email" field.
	Email string `json:"email,omitempty"`
	// CreditType holds the value of the "credit_type" field.
	CreditType string `json:"credit_type,omitempty"`
	// Amount holds the value of the "amount" field.
	Amount float64 `json:"amount,omitempty"`
	// CapturedAt holds the value of the "captured_at" field.
	CapturedAt   time.Time `json:"captured_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AICreditSnapshot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case aicreditsnapshot.FieldAmount:
			values[i] = new(sql.NullFloat64)
		case aicreditsnapshot.FieldID:
			values[i] = new(sql.NullInt64)
		case aicreditsnapshot.FieldEmail, aicreditsnapshot.FieldCreditType:
			values[i] = new(sql.NullString)
		case aicreditsnapshot.FieldCapturedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AICreditSnapshot fields.
func (_m *AICreditSnapshot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case aicreditsnapshot.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case aicreditsnapshot.FieldEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email", values[i])
			} else if value.Valid {
				_m.Email = value.String
			}
		case aicreditsnapshot.FieldCreditType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field credit_type", values[i])
			} else if value.Valid {
				_m.CreditType = value.String
			}
		case aicreditsnapshot.FieldAmount:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field amount", values[i])
			} else if value.Valid {
				_m.Amount = value.Float64
			}
		case aicreditsnapshot.FieldCapturedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field captured_at", values[i])
			} else if value.Valid {
				_m.CapturedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AICreditSnapshot.
// This includes values selected through modifiers, order, etc.
func (_m *AICreditSnapshot) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this AICreditSnapshot.
// Note that you need to call AICreditSnapshot.Unwrap() before calling this method if this AICreditSnapshot
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AICreditSnapshot) Update() *AICreditSnapshotUpdateOne {
	return NewAICreditSnapshotClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AICreditSnapshot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AICreditSnapshot) Unwrap() *AICreditSnapshot {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AICreditSnapshot is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AICreditSnapshot) String() string {
	var builder strings.Builder
	builder.WriteString("AICreditSnapshot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("email=")
	builder.WriteString(_m.Email)
	builder.WriteString(", ")
	builder.WriteString("credit_type=")
	builder.WriteString(_m.CreditType)
	builder.WriteString(", ")
	builder.WriteString("amount=")
	builder.WriteString(fmt.Sprintf("%v", _m.Amount))
	builder.WriteString(", ")
	builder.WriteString("captured_at=")
	builder.WriteString(_m.CapturedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AICreditSnapshots is a parsable slice of AICreditSnapshot.
type AICreditSnapshots []*AICreditSnapshot
